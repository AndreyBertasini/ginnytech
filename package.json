{
  "name": "ginnytech",
  "type": "module",
  "version": "2.0.0",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build && mkdir -p .open-next/assets && cp -r dist/* .open-next/assets/",
    "preview": "astro preview",
    "deploy": "npm run build && wrangler pages deploy .open-next/assets --project-name=ginnytech"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^5.18.1",
    "shiki": "^4.0.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250303.0",
    "wrangler": "^4.0.0"
  }
}
